<?php

/**
 * Implements hook_theme().
 */
function dashboard_theme($existing, $type, $theme, $path) {
  return array(
   'change_password_card' => array(
            'variables' => array('lastchanged' => NULL,),
        ),
	'employee_count_card' => array(
            'variables' => array('emp_count' => NULL,),
        ),
  );
}
